import {ContentContainer} from "../layouts";
import {EditBook} from ".";

const AddBookPage = () => {
  return (
    <ContentContainer>
      <EditBook />
    </ContentContainer>
  );
};

export default AddBookPage;
